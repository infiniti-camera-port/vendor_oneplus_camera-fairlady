# vendor_oneplus_camera-fairlady

OnePlus 15T (fairlady) per-device camera vendor config:
`camera-fairlady.mk` inherits `camera-sm8850-common.mk` and leaves payload
population to the matching proprietary camera repo.

- Build path: `vendor/oneplus/camera-fairlady`
